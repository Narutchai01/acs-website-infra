services:
  portal:
    container_name: acs-website-portal-staging
    image: ghcr.io/asc-base/acs-portal:staging-bb6058addd1b7851fb5ccbaf37751c212f48734e
    restart: unless-stopped
    environment:
      API_URL: ${API_URL}
      NODE_ENV: staging
      # PORT: ${PORTAL_PORT}
    # depends_on:
    #   servers:
    #     condition: service_healthy
